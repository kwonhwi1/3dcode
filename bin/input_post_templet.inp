start
0
end
0
n
0
mode(1:post, 2:surface pressure, 3:clcd)
0
planform area
0.d0
! name, nsteady, nexport, iturb, fluid, ngas  must check in  input.inp for postprocessing
! planform area, uref, rhoref, l_chord, aoa, aos for clcd & cp calculation

